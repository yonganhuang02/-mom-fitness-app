{
  "name": "mom-fitness-app",
  "version": "1.0.0",
  "description": "妈妈减肥打卡 - 每日体重记录应用",
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/cli": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "scripts": {
    "build": "node scripts/prepare-web.js",
    "cap:init": "npx cap add android",
    "cap:sync": "npx cap sync android"
  }
}
